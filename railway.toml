[build]
builder = "nixpacks"

[deploy]
startCommand = "pip install -r /app/requirements.txt && cd /app/backend && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn university_activities.wsgi:application --bind 0.0.0.0:$PORT --workers 4"
healthcheckPath = "/api/health"
