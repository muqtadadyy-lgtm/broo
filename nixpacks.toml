[phases.build]
cmds = [
    "cd backend && chmod +x build.sh && ./build.sh"
]

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[start]
cmd = "cd backend && gunicorn university_activities.wsgi:application --bind 0.0.0.0:$PORT"
